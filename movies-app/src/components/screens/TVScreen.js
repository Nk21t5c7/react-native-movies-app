import TVListContainer from '../containers/TVListContainer';

const TVScreen = ({ navigation }) => {

    return (
        <TVListContainer navigation={navigation} />
    )
}

export default TVScreen;


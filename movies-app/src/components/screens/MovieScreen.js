import MovieListContainer from '../containers/MovieListContainer';

const MovieScreen = ({ navigation }) => {
    return (
        <MovieListContainer navigation={navigation} />
    )
}

export default MovieScreen;